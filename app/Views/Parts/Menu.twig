<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ route('home') }}">
            <img src="{{ asset('img/logo.svg') }}" width="30" height="30" class="d-inline-block align-top mr-3" alt=""><!--
            -->{% if navbar_brand is empty %}<h1 class="h5 font-weight-normal d-inline-block">{{ config('website_name') }}</h1>{% else %}{{ navbar_brand|raw }}{% endif %}
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMobile" aria-controls="navbarMobile" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMobile">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item{{ currentRoute() is same as('home') ? ' active' : '' }}">
                    <a class="nav-link" href="{{ route('home') }}" rel="nofollow">
                        Home{% if currentRoute() is same as('home') %}<span class="sr-only">(current)</span>{% endif %}
                    </a>
                </li>
                <li class="nav-item{{ currentRoute() is same as('about') ? ' active' : '' }}">
                    <a class="nav-link" href="{{ route('about') }}">
                        About{% if currentRoute() is same as('about') %}<span class="sr-only">(current)</span>{% endif %}
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
